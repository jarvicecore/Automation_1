# Runtime dependencies. Pinned exactly -- a floating range means two builds of
# the same commit can install different code, which would make the artifact
# digest unreproducible and the promotion manifests unverifiable.
#
# Dependabot raises PRs when these move; Snyk and dependency-review scan them.
fastapi==0.140.0
uvicorn[standard]==0.51.0
