## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] `ruff check` / `ruff format --check` pass locally
- [ ] `mypy` passes locally
- [ ] Tests added or updated for the change, `pytest` passes locally
- [ ] No secrets, credentials, or internal hostnames introduced
- [ ] Docs (README/ADRs) updated if this changes behavior or architecture

## Security considerations

<!-- Does this change affect permissions, dependencies, secrets handling,
     or the CI/CD pipeline itself? Call it out here. -->
